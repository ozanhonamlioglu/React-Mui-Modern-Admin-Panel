import "./index.css";
import FullPageContainer from "components/flex/FullPageContainer";

const Login = () => {
	return <FullPageContainer className="gradient-bg"></FullPageContainer>;
};

export default Login;
